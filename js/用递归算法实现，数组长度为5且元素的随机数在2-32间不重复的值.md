```javascript

  let array = [];
  function Random(){
    if( array.length < 5 ){
      let number = Math.floor(Math.random() * 30) + 2;
      if( array.indexOf(number) == -1){
        array.push(number);
        Random();
      }
    }
  }

```